// Next.js build configuration for the WorldCupOS frontend.
// Exports default config — extend here for redirects, images, or env passthrough.
/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
};

export default nextConfig;
